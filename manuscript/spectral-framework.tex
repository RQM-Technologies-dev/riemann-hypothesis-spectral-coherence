This section defines a minimal formal interface for the manuscript's proposed operator/coherence layer.
It does not claim that the layer is complete.
Its role is to make explicit which properties must be supplied before downstream RH-reduction claims can be treated as theorem-level statements.

\subsection*{Current formal status and scope}
\placeholder{A finalized operator-level construction has not yet been fixed.}
The symbols $H$ and $\Phi$ are currently part of the manuscript's formalization agenda:
\begin{itemize}
  \item $H$ denotes a proposed spectral object (operator/system) intended to encode the manuscript's symmetry/coherence mechanism.
  \item $\Phi$ denotes a proposed balance/coherence functional extracted from the finalized $H$-layer.
\end{itemize}
At present, these are not yet complete theorem-level objects in the sense required for Stages 2--3 of the proof architecture.

\subsection*{Minimal required structure for the $H$-layer}
The manuscript does not need every possible operator-theoretic feature.
It does need the following \emph{minimal interface} to keep later arguments well-typed and auditable.

\paragraph{(M1) Ambient space and typing data.}
A specified complex topological vector space (typically a Hilbert or Banach model) $\mathcal{X}$, together with clear codomain conventions for all derived maps.
Every use of $H$ must state its typing context explicitly.

\paragraph{(M2) Domain and well-defined action.}
A dense or otherwise explicitly justified domain $\mathcal{D}(H)\subseteq\mathcal{X}$ (as required by the chosen model), and a map
\[
H:\mathcal{D}(H)\to\mathcal{X}
\]
with proof that this action is well defined under stated hypotheses.
If closure/closability is needed by subsequent lemmas, that requirement must be stated and proved where used.

\paragraph{(M3) Symmetry-compatibility package.}
A precise compatibility statement linking the operator layer to the reflection involution $\sigma(s)=1-s$ from \texttt{definitions.tex}.
This may be invariance, covariance, intertwining, or another concrete relation, but it must be explicit and non-heuristic.
Informal symmetry pictures are admissible only as motivation, not as substitute for this package.

\paragraph{(M4) Operator-regularity assumptions (structural side).}
The exact regularity required for operator statements: continuity/measurability/analytic dependence of operator-valued maps, domain persistence, and closure/graph conditions needed for later operator lemmas.
Each assumption must be tagged as either proved in-manuscript or imported as an explicit hypothesis.

\paragraph{(M5) Dependency traceability.}
A lemma-level dependency map from operator properties to later claims (especially Stages 3--6 in \texttt{proof-architecture.tex}).
No RH-relevant step should rely on an unlisted implicit property of $H$.

\subsection*{How to read M1--M5: requirement classes}
For audit, the $H$-layer requirements split into two roles.
\begin{itemize}
  \item \textbf{Structural interface requirements:} (M1)--(M3). These fix object type, operator action, and symmetry-compatibility form.
  \item \textbf{Operator-regularity/trace requirements:} (M4)--(M5). These ensure the operator layer is usable without hidden domain or closure assumptions.
\end{itemize}
This split is not cosmetic: Stages 2--3 in \texttt{proof-architecture.tex} require both classes to be complete before Target A/B bridges can be promoted beyond placeholders.

\subsection*{Minimal required structure for the coherence functional $\Phi$}
Once a candidate $H$-layer is fixed, $\Phi$ must be introduced as a derived object with explicit dependence.

\paragraph{(P1) Definition from finalized inputs.}
A formula or construction rule
\[
\Phi = \Phi_H\quad\text{(or }\Phi(s)=\Phi_H(s)\text{)}
\]
whose inputs are only previously defined data.
The definition must be non-circular and must not presuppose the zero set of $\zeta$.

\paragraph{(P2) Domain of evaluation (admissible class).}
A specified admissible class $\mathcal{A}\subseteq\C$ consistent with \texttt{definitions.tex}, including all side conditions needed for finiteness and well-posedness.
If $\Phi$ is only partially defined, the excluded region must be explicit.

\paragraph{(P3) Well-posedness statement.}
For $s\in\mathcal{A}$, proof that $\Phi_H(s)$ exists and is finite, plus the exact continuity/regularity properties later used in correspondence and uniqueness steps.
Computational checks may support plausibility but do not satisfy this requirement.

\paragraph{(P4) Symmetry behavior of $\Phi$.}
A formal statement of how $\Phi$ transforms under $s\mapsto1-s$ (invariance, sign change, conjugation relation, etc.), derived from the stated $H$-properties.
This is required for any reflection-based cancellation argument.

\paragraph{(P5) Bridge interface to zero claims.}
A precise theorem target describing what must be shown about
\[
\Phi_H(s)=0
\]
and how that condition maps to encoded zeta-zero statements.
This interface should separate:
\begin{enumerate}
  \item the forward direction to be proved,
  \item the converse direction to be proved,
  \item side conditions preventing spurious balanced points.
\end{enumerate}

\subsection*{How to read P1--P5: requirement classes}
For the coherence functional, the same two-way split is used.
\begin{itemize}
  \item \textbf{Structural interface requirements:} (P1), (P2), and the transformation law in (P4). These determine what $\Phi_H$ is, where it is evaluated, and how symmetry acts on it.
  \item \textbf{Regularity/control requirements:} (P3) and the side-condition component of (P5). These prevent undefined values, unstable inference, and spurious balance points.
\end{itemize}
Under this organization, correspondence and exclusion arguments may use only the declared structure/control items, with no hidden assumptions.


\subsection*{Analytic-control theorem package (AC) for transfer and exclusion}
The manuscript separates operator closure from analytic control.
Operator closure concerns the existence and symmetry typing of $H$ and $\Phi_H$.
Analytic control concerns whether local estimates can be transported across $\mathcal{A}_{\mathrm{strip}}$ without losing admissibility or creating hidden balance branches.

The required analytic package is recorded as open theorem targets, not as proved results.
Let $\mathcal{A}_{\mathrm{strip}}:=\{s\in\mathcal{A}:0<\RePart(s)<1\}$ and $\mathcal{B}:=\{s\in\mathcal{A}_{\mathrm{strip}}:\Phi_H(s)=0\}$.

\paragraph{(AC1) Strip-wide growth and defect bounds.}
A theorem-level bound class controlling the quantities used in Stage~6 contradiction arguments, uniform across boundary-near and large-parameter sectors.
This includes explicit constants/rates and a statement of where each bound is valid.

\paragraph{(AC2) Uniform convergence and regime compatibility.}
If $\Phi_H$ or auxiliary objects are defined by limits/series/integrals, one needs convergence claims that are uniform on the subsets used for local-to-global continuation.
Pointwise convergence alone is insufficient for transfer.

\paragraph{(AC3) Interchange legitimacy.}
Every interchange used by the argument (limit--integral, derivative--integral, summation--integration, contour deformation) must be justified under explicit hypotheses, with the admissible class stated.

\paragraph{(AC4) Continuation/extension control.}
A continuation principle must show that estimates proved in $\mathcal{R}_{\mathrm{loc}}$ extend to the declared global sectors of $\mathcal{A}_{\mathrm{strip}}$ with no loss of hypothesis synchronization.
If compactness/normal-family arguments are used, the required precompactness or closure inputs must be stated.

\paragraph{(AC5) No-hidden-branch control for $\mathcal{B}$.}
A theorem target must exclude emergence of off-line components of $\mathcal{B}$ outside the local regime due solely to analytic degeneration, loss of uniformity, or boundary escape.
This is the analytic side of the no-alternative-locus burden.

\paragraph{(AC6) Local-estimate compatibility with strip admissibility.}
The local coercive/rigidity/positivity estimates used for Target~B must be shown compatible with the full-strip admissibility constraints (not just with a narrower local model).

\paragraph{Status note.}
(AC1)--(AC6) are open analytic bridges in the current manuscript version.
They define the analytic-control burden required for Stage~6 closure but are not claimed proved here.

\subsection*{Dependency use map to later stages and lemma targets}
The interface above is designed to feed specific downstream bridges.

\paragraph{(i) Inputs that directly feed Target A (Stage 4).}
\begin{itemize}
  \item \textbf{From $H$:} (M3) for reflection-compatible transport; (M4) for any limit/interchange operations used in the correspondence map; (M5) to keep directional claims (forward/converse) explicitly separated.
  \item \textbf{From $\Phi$:} (P1)--(P3) to define a non-circular balance predicate on an admissible class; (P5) to state the bidirectional theorem target and spurious-point controls.
  \item \textbf{Primary lemma targets in \texttt{lemmas.tex}:} B1 well-posedness targets, B2 reflection/conjugation compatibility targets, and B3 zero--balance correspondence targets.
\end{itemize}

\paragraph{(ii) Inputs that directly feed Target B (Stage 5).}
\begin{itemize}
  \item \textbf{From $H$:} (M3)--(M4), providing the symmetry and local coercive/control ingredients used to force localization to $\RePart(s)=\tfrac12$ in a declared regime.
  \item \textbf{From $\Phi$:} (P3)--(P5), especially side-condition discipline needed to distinguish genuine balance points from artefacts and to define the local regime precisely.
  \item \textbf{Primary lemma targets in \texttt{lemmas.tex}:} B4 local balance-axis uniqueness target (not yet global exclusion), with dependencies on B1--B3.
\end{itemize}

\paragraph{(iii) Inputs that directly feed Stage 6 off-line exclusion (global).}
\begin{itemize}
  \item \textbf{From $H$:} (M4)--(M5), to justify extension from local regimes to strip-wide control and to document exactly which operator properties are invoked globally.
  \item \textbf{From $\Phi$:} (P3)--(P5), where global exclusion needs both well-posedness and explicit side-condition closure across the entire admissible strip.
  \item \textbf{Transfer-bridge requirement (explicit):} a theorem-level local-to-global principle carrying Stage-5 uniqueness from $\mathcal{R}_{\mathrm{loc}}$ to $\mathcal{A}_{\mathrm{strip}}$, including admissibility stability, continuation/compactness uniformity, symmetry compatibility, and no-new-off-line-branch control.
  \item \textbf{Primary lemma targets in \texttt{lemmas.tex}:} B4 admissibility-stability transfer, uniform continuation/compactness control, no-new-off-line-branch emergence, global-extension target, no-alternative-locus target for $\mathcal{B}$, contradiction-route exclusion target, and full-strip no-off-line-balance target, plus quartet-compatible transfer, with B5 operator/analytic-control support.
\end{itemize}

\paragraph{(iv) Stage alignment summary.}
\begin{itemize}
  \item \textbf{Stage 2 (\texttt{proof-architecture.tex}):} anchored by (M1)--(M4), with B5 operator-closure support.
  \item \textbf{Stage 3:} anchored by (P1)--(P4), with B1 admissibility/well-posedness support.
  \item \textbf{Stage 4 / Target A:} uses (M3)--(M5) and (P1)--(P5), with B2--B3 bridges.
  \item \textbf{Stage 5 / Target B:} uses (M3)--(M5) and (P3)--(P5), with B4 local uniqueness bridge.
  \item \textbf{Stage 6:} uses operator trace inputs (M4)--(M5), functional controls (P3)--(P5), and analytic package (AC1)--(AC6), with B4 global-extension/full-strip exclusion bridges and B5 control bridges.
\end{itemize}
These alignments specify dependency direction only. They do not upgrade any open bridge to proved status.

\subsection*{Available structure vs. required open structure}
\paragraph{Currently available structure (documented).}
\begin{itemize}
  \item Classical objects and symmetry notation (including $\xi$, critical strip/line, and $\sigma$) are fixed in \texttt{definitions.tex}.
  \item The proof program and stage-level dependency chain are fixed in \texttt{proof-architecture.tex}.
  \item Gap categories and closure criteria are explicitly listed in \texttt{known-gaps.md}.
\end{itemize}

\paragraph{Required but not yet proved structure (open bridges).}
\begin{itemize}
  \item \textbf{Exact spectral object.} A single finalized construction satisfying (M1)--(M5).
  \item \textbf{Zero--balance correspondence.} A fully non-circular, theorem-level correspondence package (Target A) with both directions and explicit side-condition control.
  \item \textbf{Uniqueness of the balance axis.} A theorem-level localization result (Target B) proving that admissible balanced points lie only on $\RePart(s)=\tfrac12$.
  \item \textbf{Exclusion of off-critical-line zeros.} A full-strip exclusion package that upgrades local arguments to the entire admissible critical strip.
  \item \textbf{Operator-theoretic gaps.} Proof-level closure of domain, symmetry/intertwining, and spectral-legitimacy requirements used by the correspondence and exclusion steps.
  \item \textbf{Analytic-control gaps.} Proof-level closure of growth, convergence, and interchange-of-limits requirements used by $\Phi$ and downstream reduction steps.
\end{itemize}
\openbridge{Until these six bridges are proved under synchronized hypotheses, references to ``balanced points'' or ``coherence zeros'' remain conditional framework language.}

\subsection*{Why this layer is necessary for the RH reduction}
The final reduction step in \texttt{proof-architecture.tex} is conditional on intermediate bridges that are operator- and functional-level statements.
Without a rigorous $H$/$\Phi$ layer:
\begin{enumerate}
  \item the balance condition is not a theorem-level predicate on a defined domain,
  \item the correspondence with zeta zeros cannot be stated non-circularly,
  \item the uniqueness/exclusion arguments have no formal object to which estimates apply.
\end{enumerate}
Therefore this layer is not optional technical decoration; it is a prerequisite interface for Targets A/B and for any valid RH reduction claim inside the manuscript's framework.

\begin{heuristic}
Geometric or cancellation intuition can guide candidate constructions of $H$ and $\Phi$.
However, such intuition remains heuristic until converted into explicit definitions, hypotheses, and proved lemmas matching the minimal interface above.
\end{heuristic}
