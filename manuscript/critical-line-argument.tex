This section records only the canonical critical-line exclusion target used in the main spine.
Detailed Stage~6 mechanism design (transfer templates, defect quantities, repair chains, and route comparison) is moved to technical notes so that the main manuscript remains readable for audit.

The balance object in this section is the explicit main-text candidate
\[
\Phi_H(s)=\Tr_{\mathrm{reg}}(H-sI)^{-1}-\Tr_{\mathrm{reg}}(H-(1-s)I)^{-1},
\]
defined on the admissible class $\mathcal{A}$ from \texttt{definitions.tex}.

\subsection*{Canonical exclusion target (main theorem target)}
\begin{drafttheorem}[Target B: canonical exclusion of off-line balance]
\label{thm:stage6-master-exclusion-target}
\placeholder{Theorem target under construction; proof not yet provided.}
Let
\[
\mathcal{A}_{\mathrm{strip}}:=\{s\in\mathcal{A}:0<\RePart(s)<1\},
\qquad
\mathcal{B}:=\{s\in\mathcal{A}_{\mathrm{strip}}:\Phi_H(s)=0\}.
\]
Under one explicit synchronized hypothesis package $\mathcal{H}_{\mathrm{excl}}$, the argument attempts to prove the single main-text exclusion statement
\[
\mathcal{B}\subseteq\{s\in\mathcal{A}_{\mathrm{strip}}:\RePart(s)=\tfrac12\}.
\]
Equivalently, the argument attempts to show that zeros of this explicit resolvent reflection-defect functional inside the admissible strip class occur only on $\RePart(s)=\tfrac12$.
This theorem target is open in the current manuscript version.
\end{drafttheorem}

\begin{remark}[Main-text statement policy]
Set-theoretic complements, quantified forms, and contradiction-style restatements of Target~B are treated as supporting formulations.
They are intentionally omitted from this section to keep one canonical exclusion target in view.
\end{remark}

\subsection*{How this section interfaces with the RH reduction}
\begin{proposition}[Use of the exclusion target in the proof spine]
If \cref{thm:target-correspondence} and \cref{thm:stage6-master-exclusion-target} are both proved under compatible assumptions, then the RH reduction in \cref{prop:rh-reduction-main-spine} follows.
\end{proposition}

\begin{remark}[Audit boundary]
This section states the endpoint needed from the critical-line argument.
It does not claim that transfer-to-global closure, regularization-control lemmas, or any specific contradiction mechanism has been proved.
Those remain open technical burdens documented outside the main proof spine.
\end{remark}
